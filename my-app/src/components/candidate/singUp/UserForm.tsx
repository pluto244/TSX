import React, { ChangeEvent } from "react";
import { observer } from "mobx-react";
import singUpStore from "../../../stores/EmailStore";

const SingUp = () => {
  <div></div>
}

export default (SingUp);
